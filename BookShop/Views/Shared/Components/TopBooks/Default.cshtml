@model IEnumerable<BookModel>
<div class="container">    
    <div class="row">
        @foreach (var book in Model)
        {
            <div class="col-md-4 ">
                <partial name="_ShowingBook" model="@book" />
            </div>
        }
    </div>
</div>           